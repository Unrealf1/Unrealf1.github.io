set nu " add line numbers
set expandtab " spaces instead of tabs
set tabstop=4 " tab width in spaces
set shiftwidth=4 " indent width in spaces
set smartindent " smart tabs
set smartcase " search case insensitive unless you put a capital letter or \C in your search, and then it will become case sensitive again.
set mouse=a " enable full mouse support
set cursorline " add horizontal line under the current line
set scrolloff=3 " add offset before scrolling
setlocal foldmethod=syntax " enable folding (za, zc, zo)
syntax on " enable syntax highlight
set incsearch " While searching though a file incrementally highlight matching characters as you type.
set hlsearch " Highlight matches during search
set enc=utf-8


" ---  vundle stuff --- UNCOMMENT LINES BELOW THAT START WITH " TO USE PLUGINS!
"set nocompatible              " be iMproved, required
"filetype off                  " required

" set the runtime path to include Vundle and initialize
"set rtp+=~/.vim/bundle/Vundle.vim
"call vundle#begin()

"Plugin 'VundleVim/Vundle.vim'
"Plugin 'ycm-core/YouCompleteMe'

"call vundle#end()            " required
"filetype plugin indent on    " required
